export interface ILocalWarehouse{
    idGlobalWarehouse: number,
    city: string,
    street: string,
    number: number,
    postalCode: string,
    active:boolean,
    globalWarehouseId: number
  }